{
  "name": "gantry",
  "version": "1.0.0",
  "description": "MCP proxy server and fleet web dashboard for Claude Code agent fleets",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "server"
  ],
  "scripts": {
    "build": "bun run --cwd server build",
    "build:server": "bun run --cwd server build:server",
    "test": "bun run --cwd server test",
    "start": "bun run --cwd server start"
  },
  "devDependencies": {
    "redent": "3.0.0"
  },
  "overrides": {
    "fast-uri": "^3.1.2",
    "path-to-regexp": "^8.4.2",
    "hono": "^4.12.21",
    "@hono/node-server": "^1.19.10",
    "ip-address": "^10.2.0",
    "lodash-es": "^4.18.1",
    "express-rate-limit": "^8.2.2",
    "postcss": "^8.5.10",
    "ws": "^8.20.1"
  }
}